---
"@runfusion/fusion": patch
---

Fix dashboard file browser editor rendering both a plain textarea and the
CodeMirror editor pane simultaneously — only the syntax-highlighted CodeMirror
pane now renders.
